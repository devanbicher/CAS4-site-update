#!/bin/bash

#this script is responsible for doing the site transfer,  this should probably just be copying the backup file to the site folder, then doing the migration command. then pulling the theme down from git.
# I will try to automate as much of this as possible I suppose.

